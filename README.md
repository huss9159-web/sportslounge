# Sports Lounge — Booking (Node + Express + SQLite)

## Quick start

1. Install dependencies:
   ```
   npm install
   ```

2. Start:
   ```
   npm start
   ```

3. Open: `http://localhost:3000`

## Default admin
- username: `admin`
- password: `1234`

## Seed data
This package includes seed data (one sample user and one sample booking) in `db.sqlite`.

## Notes
- For production, add proper password hashing and secure authentication (JWT/cookies).
